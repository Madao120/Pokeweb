{
  "name": "pokeweb",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c green,blue \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "mvn -f backend/demo/pom.xml spring-boot:run"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
